# Build and Release Folders
bin/
bin-debug/
bin-debug-temp/
bin-release/
bin-release-temp/
workerswfs/

# Other files and folders
*.swf
.settings/
.vscode/

# Certificates (security sensitive)
*.p12

# Project files, i.e. `.project`, `.actionScriptProperties` and `.flexProperties`
# should NOT be excluded as they contain compiler settings and other important
# information for Eclipse / Flash Builder.

# SDKs are included, but exclude large files that exceed GitHub limits
# GitHub limit: 100MB per file
libs/**/AdobeAIRSDK.zip
libs/**/*.a
libs/**/lib/aot/
libs/**/in/airsdk/
libs/**/in/AdobeAIRSDK.zip

# Keep essential SDK files but exclude heavy AOT/native libraries
libs/**/frameworks/
libs/**/bin/
libs/**/templates/
libs/**/samples/
libs/**/runtimes/air/

# node_modules
node_modules/
